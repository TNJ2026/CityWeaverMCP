import {Client} from '@modelcontextprotocol/sdk/client/index.js';import {StdioClientTransport} from '@modelcontextprotocol/sdk/client/stdio.js';import {writeFile} from 'node:fs/promises';
const c=new Client({name:'harbor-detail-readonly',version:'1'});async function call(name,args){const r=await c.callTool({name,arguments:args},undefined,{timeout:120000});if(!r.structuredContent?.ok)throw Error(JSON.stringify(r));return r.structuredContent.data}
try{await c.connect(new StdioClientTransport({command:process.execPath,args:[new URL('./server.mjs',import.meta.url).pathname.replace(/^\/(\w:)/,'$1')]}));let out={cells:[]},offset=0;do{let r=await call('read_surface_water_mask',{bounds:{min_x:1800,max_x:5300,min_z:1900,max_z:3428},cell_size_m:16,offset,limit:1024});out.cells.push(...r.cells);offset=r.next_offset;}while(offset!=null);await writeFile('artifacts/auburn-harbor-detail.json',JSON.stringify(out));console.log(out.cells.length)}finally{await c.close()}
